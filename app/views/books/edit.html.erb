<h1>Editing Book</h1>
<%= form_for(@show) do |f| %>
  <p>Title</p>
  <%= f.text_field :title %>
  <p>Body</p>
  <%= f.text_area :body %>
  <%= f.submit 'Update Book' %>
<% end %>
<p><%= link_to 'Show', book_path(@show.id) %></p>
<p>|</p>
<p><%= link_to 'Back', '/books' %></p>